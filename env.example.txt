# FoodieMap environment template
# Copy this file to `.env` and replace the placeholder values.

# Required in every environment. Must be at least 32 characters.
SESSION_SECRET=replace-with-a-random-secret-at-least-32-characters

# development or production. Production should use PostgreSQL and GCS.
APP_ENV=development

# Public base URL used for OAuth redirects and generated share links.
# Local default:
APP_BASE_URL=http://localhost:5174

# Optional Google OAuth login. Leave blank to disable Google sign-in.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional Google Geocoding API key. Used to fill an address when a Maps link only has coordinates.
GOOGLE_GEOCODING_API_KEY=

# Dedicated admin portal at /admin. Configure both values to enable it.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-a-strong-admin-password

# Free account restaurant storage limit.
FREE_RESTAURANT_LIMIT=50

# Optional email delivery for one-time email codes and password reset.
# Leave SMTP_HOST blank to disable email code delivery.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_USE_TLS=true

# Local development storage. Defaults are suitable for local SQLite development.
# DATA_DIR=data
# UPLOAD_DIR=data/uploads
# SQLITE_DATABASE_PATH=data/foodiemap.db

# Production database. Use a Cloud SQL PostgreSQL connection string.
# Example for Cloud Run Cloud SQL Unix socket:
# DATABASE_URL=postgresql://foodiemap_user:PASSWORD@/foodiemap?host=/cloudsql/PROJECT_ID:REGION:INSTANCE
DATABASE_URL=

# Production object storage. When set, uploads are stored in Google Cloud Storage.
GCS_BUCKET=
# Optional public base URL or CDN origin. If empty, the app serves /uploads/* by proxying GCS.
GCS_PUBLIC_BASE_URL=

# Optional server port when running `python3 server.py` without an argument.
# PORT=5174
